[project]
name = "load-balancer"
version = "0.1.0"
description = "Advanced Load Balancer Dashboard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
